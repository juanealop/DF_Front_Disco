// Espejo de RespuestaAutenticacion del backend (com.DF.back.shared.security.dto).
// Ajustar según los campos reales que devuelva el backend si se necesitan.
export interface RespuestaAutenticacion {
  token: string;
}
